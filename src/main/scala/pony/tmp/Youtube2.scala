package pony.tmp

/**
  * Created by HamsterofDeath on 7/8/2016.
  */
object Youtube2 {
  def main(args: Array[String]): Unit = {
    def dst = {
      val a = math.random - math.random
      math.sqrt((a * a + b * b))
    }
    val it = Iterator.continually(dst)
    val count = 1000000 * 1
    val avg = it.take(count).sum / count
    println(avg)

  }
}
